Do this now, in order

~~1. Check exactly what changed~~
~~From repo root:~~

~~cd ~/Desktop/VeriFit~~

~~git status --short~~
~~git diff --stat~~
~~git diff~~
~~At this point, changes should be Task 0.6-related.~~

~~2. Make sure Task 0.5 was not modified~~
~~Run:~~

~~git diff -- packages/database/prisma/schema.prisma~~
~~git diff -- packages/database/prisma/migrations/~~
~~git diff -- docker-compose.yml~~
~~git diff -- apps/worker/src/config/redis.ts~~
~~These should be empty unless there is a deliberate, documented reason.~~

~~Also verify the Redis policy:~~

~~docker compose exec redis redis-cli CONFIG GET maxmemory-policy~~
~~It must say:~~

~~noeviction~~
~~Do not accept the 0.6 document's allkeys-lru claim. That document is inconsistent with your actual Task 0.5 state.~~

~~3. Run the complete local quality gate~~
~~pnpm install --frozen-lockfile~~
~~pnpm run typecheck~~
~~pnpm run lint~~
~~pnpm run format:check~~
~~pnpm run test~~
~~pnpm run build~~
~~Every one must pass.~~

~~The task itself requires these checks as part of its verification.~~

~~4. Verify the CI workflow itself~~
~~Check:~~

~~ls -la .github/workflows/~~
~~cat .github/workflows/ci.yml~~
~~We want the workflow to contain the intended gates:~~

~~quality~~
~~tests~~
~~build~~
~~dependency security~~
~~SAST~~
~~secret scanning~~
~~database migration testing~~
~~Docker Compose validation~~
~~final gate~~
~~The task defines those eight jobs.~~

~~But don't worry if the exact job names differ slightly; functionality matters.~~

~~5. Check for the old CI mistakes~~
~~Run:~~

~~grep -RInE \\~~
~~'returntocorp/semgrep-action|trufflehog@main|docker-compose |allkeys-lru|workers\\.count|workers\\.active' \\~~
~~.github docs scripts README.md \\~~
~~--exclude-dir=node_modules \\~~
~~--exclude-dir=.git~~
~~Ideally this returns nothing relevant.~~

~~Also check old database credentials:~~

~~grep -RInE \\~~
~~'verifit_user|verifit_dev' \\~~
~~. \\~~
~~--exclude-dir=node_modules \\~~
~~--exclude-dir=.git~~
~~Ideally no relevant matches.~~

~~6. Check the new documentation exists~~
~~test -f SECURITY.md && echo "SECURITY.md ✅"~~
~~test -f docs/PRODUCTION_READINESS.md && echo "Production readiness ✅"~~
~~test -f docs/PERFORMANCE_BASELINE.md && echo "Performance targets ✅"~~
~~test -f docs/DEPLOYMENT_CHECKLIST.md && echo "Deployment checklist ✅"~~

~~test -f docs/runbooks/DATABASE_FAILURES.md && echo "DB runbook ✅"~~
~~test -f docs/runbooks/REDIS_FAILURES.md && echo "Redis runbook ✅"~~
~~test -f docs/runbooks/DEPLOYMENT.md && echo "Deployment runbook ✅"~~
~~The task expects these operational documents.~~

~~7. Check the documentation for stale commands~~
~~Because the 0.6 document repeatedly contains old docker-compose syntax and old DB credentials, search the actual files:~~

~~grep -RInE \\~~
~~'docker-compose|verifit_user|verifit_dev|allkeys-lru' \\~~
~~README.md SECURITY.md docs/ \\~~
~~--exclude-dir=node_modules~~
~~We want the documentation to describe our actual repository, not the original task.~~

~~8. Validate Docker configuration~~
~~Even though 0.5 was already tested, 0.6 added CI validation around it:~~

~~docker compose config~~
~~Then:~~

~~docker compose ps~~
~~Both services should be healthy.~~

~~9. Test migrations in a clean environment~~
~~This is particularly important because 0.6 adds migration testing to CI. The task's CI specifically creates a fresh PostgreSQL service and runs migrations.~~

~~Locally, verify:~~

~~cd packages/database~~
~~pnpm prisma migrate status~~
~~pnpm prisma validate~~
~~pnpm prisma generate~~
~~cd ../..~~
~~No migration errors.~~

~~Do not modify the existing migration just to make CI pass.~~

~~10. Inspect the security workflow carefully~~
~~The task adds dependency scanning, SAST, and secret scanning.~~

~~Check:~~

~~grep -nE 'audit|semgrep|truffle|codeql|sarif|security' \\~~
~~.github/workflows/ci.yml~~
~~We want:~~

~~dependency scan ✅~~
~~SAST ✅~~
~~secret scan ✅~~
~~but also make sure the actions are using current supported versions, not the obsolete versions from the original task.~~

11. Verify GitHub Actions for real
    This is the most important part of 0.6.

Local tests only prove the code works locally.

Task 0.6 is about CI/CD, so we need an actual GitHub Actions run.

Once the workflow is committed and pushed, go to:

GitHub → VeriFit → Actions
and verify the workflow actually runs and all required jobs succeed.

We want:

quality ✅
tests ✅
build ✅
security-deps ✅
security-sast ✅
security-secrets ✅
db-migrations ✅
infra-docker ✅
gate ✅
The task explicitly makes the final gate depend on those jobs.

One important documentation correction
Do not allow the code giver to write:

Phase 0 complete
Task 0.6 complete
before the GitHub Actions run has actually passed.

The task currently tells it to update project status as though completion is already established.

Completion should be:

implementation
↓
local verification
↓
commit
↓
push
↓
GitHub Actions passes
↓
THEN mark Phase 0 complete
~~After all that~~
~~When everything passes:~~

~~git status~~
~~git diff --check~~
~~Then commit and push:~~

~~git add .~~
~~git commit -m "chore: harden foundation ci and security"~~
~~git push origin main~~
~~Then wait for GitHub Actions to finish.~~

Your final 0.6 lock gate
~~Local typecheck ✅~~
~~Local lint ✅~~
~~Local format ✅~~
~~Local tests ✅~~
~~Local build ✅~~
~~Docker validation ✅~~
~~Migration validation ✅~~
~~Security workflow present ✅~~
~~SAST workflow present ✅~~
~~Secret scanning present ✅~~
~~No stale commands ✅~~
~~No stale credentials ✅~~
~~Task 0.5 unchanged ✅~~
GitHub Actions ⏳ (pushed — waiting for run)
Final CI gate ⏳
Do these checks first. Don't change anything just because you see a difference; show me the actual outputs.

The most useful next message from you is the output of:

git status --short
git diff --stat
pnpm run typecheck
pnpm run lint
pnpm run format:check
pnpm run test
pnpm run build
plus the GitHub Actions result once it runs.
