# Prisma CLI environment
# Used by: prisma migrate dev, prisma generate, prisma studio
# For local dev, copy root .env.example to root .env.local — Prisma CLI picks it up automatically.
# Only create this file if you need to override the root value for Prisma CLI specifically.

DATABASE_URL=postgresql://verifit:verifit@localhost:5432/verifit
