# apps/api — local override example.
# For local dev, copy the root .env.example to root .env.local instead.
# Only create this file to override specific API variables.
NODE_ENV=development
API_PORT=3001
DATABASE_URL=postgresql://verifit:verifit@localhost:5432/verifit
CORS_ORIGINS=http://localhost:3000
API_DOCS_ENABLED=true
LOG_LEVEL=info
